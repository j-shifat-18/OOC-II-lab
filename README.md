# 📚 Library Management System (OOP in Java)




## 🚀 Features

- **Abstract Class**: `Person` with common attributes and an abstract method.
- **Inheritance**: `Student` and `Faculty` extend `Person`.
- **Composition**: Each `Student` and `Faculty` has a collection of `Book` objects.
- **Static Variable**: `Book.totalBooksIssued` tracks total issued books.
- **Runtime Polymorphism**: `Library.printPersonDetails()` accepts any `Person` type.
- **Constructors**: Both default and parameterized constructors implemented.
- **Clean, modular code** following OOP best practices.

---

## 📦 Class Diagram (UML)

![UML Diagram](/asset/uml.jpg)

